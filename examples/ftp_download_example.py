from pyfunc2.config.ftp_download import ftp_download

if __name__ == "__main__":
    # Przykład: pobieranie danych przez FTP (funkcja wymaga prawdziwych danych)
    ftps = [
        {"target": "ftp.example.com", "user": "user", "password": "pass"}
    ]
    storage_root = "/home/tom/storage"
    # ftp_download(ftps, storage_root)
    print("ftp_download: przykład wywołania przygotowany, odkomentuj linię aby wykonać operację na prawdziwych danych.")
