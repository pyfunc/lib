from pyfunc2.config.ftp_update import ftp_update

if __name__ == "__main__":
    # Przykład: aktualizacja danych przez FTP (funkcja wymaga prawdziwych danych)
    ftps = [
        {"target": "ftp.example.com", "user": "user", "password": "pass"}
    ]
    storage_root = "/home/tom/storage"
    # ftp_update(ftps, storage_root)
    print("ftp_update: przykład wywołania przygotowany, odkomentuj linię aby wykonać operację na prawdziwych danych.")
